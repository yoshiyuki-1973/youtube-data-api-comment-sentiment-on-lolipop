# ディレクトリ構成

YouTube コメント感情分析システムのディレクトリ構成を示す。  
現行システムは **ロリポップ共有サーバーへ FTP で配置する PHP アプリケーション** であり、Docker によるローカル環境構築にも対応している。

---

## 1. ルート構成

```text
youtube-data-api-comment-sentiment-on-lolipop/
├── php/                        # メインアプリケーション
├── docs/                       # 設計・運用ドキュメント
├── docker-compose.yml          # Docker ローカル環境定義
├── .gitignore                  # Git 除外設定
├── README.md                   # プロジェクト概要
└── CLAUDE.md                   # 開発補助ガイド
```

---

## 2. php/ - メインアプリケーション

```text
php/
├── index.php                   # メイン画面（HTML + AJAX 起点）
├── Dockerfile                  # Docker イメージ定義（PHP 8.3 + Apache）
├── config.php.example          # 設定サンプル・ロリポップ用（Git 管理対象）
├── config.php.docker.example   # 設定サンプル・Docker 用（Git 管理対象）
├── config.php                  # 実設定ファイル（Git 管理外）
├── composer.json               # Composer 設定
├── composer.lock               # Composer ロックファイル
├── phpunit.xml                 # PHPUnit 設定
├── .htaccess                   # src/ / sql/ / config.php への直接アクセス制御
├── api/
│   └── analyze.php             # AJAX エンドポイント
├── src/
│   ├── Config.php              # 設定ロード
│   ├── Database.php            # PDO 接続
│   ├── Exceptions.php          # カスタム例外
│   ├── GeminiClient.php        # Gemini API 呼び出し
│   ├── SentimentAnalyzer.php   # 感情分析集計
│   ├── VideoRepository.php     # MySQL CRUD / キャッシュ参照
│   └── YouTubeClient.php       # YouTube API 呼び出し
├── assets/
│   ├── css/
│   │   └── style.css           # 画面スタイル
│   └── js/
│       └── app.js              # フロント制御・Chart.js 描画
├── sql/
│   └── schema.sql              # MySQL テーブル定義
└── tests/
    ├── bootstrap.php
    ├── GeminiClientTest.php
    ├── SentimentAnalyzerTest.php
    └── YouTubeClientTest.php
```

### 主要ファイルの役割

| ファイル | 役割 |
|---|---|
| `index.php` | 入力フォーム・結果表示・フロント初期化 |
| `api/analyze.php` | リクエスト受信、バリデーション、分析実行、JSON 応答 |
| `src/YouTubeClient.php` | URL / 動画 ID 解釈、コメント取得 |
| `src/GeminiClient.php` | コメント前処理後に Gemini API へ送信 |
| `src/SentimentAnalyzer.php` | スコア集計と表示用データ生成 |
| `src/VideoRepository.php` | `videos` / `analysis_results` / `comments` の保存・検索 |
| `tests/` | PHPUnit テストコード |

---

## 3. docs/ - ドキュメント群

```text
docs/
├── 01_企画書.md
├── 02_要件定義書.md
├── 03_技術スタック.md
├── 04_基本設計書.md
├── 05_詳細設計書.md
├── 06_データ仕様書.md
├── 07_テーブルレイアウト.md
├── 08_AI仕様書.md
├── 09_ディレクトリ構成.md
├── 10_テスト仕様書.md
├── 11_VSCode開発マニュアル.md
├── 12_Docker環境構築ガイド.md
├── 13_デプロイ手順書.md
├── 14_運用手順書.md
└── 15_操作マニュアル.md
```

---

## 4. デプロイ対象

ロリポップへ配置する対象は `php/` 配下である。  
詳細手順は [13_デプロイ手順書.md](13_デプロイ手順書.md) を参照すること。

### アップロード対象

```text
php/
├── index.php
├── api/
├── src/
├── assets/
├── sql/                # 参照用。標準は phpMyAdmin で schema.sql を実行
├── config.php          # 手動作成・手動アップロード
├── .htaccess
└── composer.json
```

### 本番不要

| パス | 理由 |
|---|---|
| `tests/` | テストコードのため |
| `phpunit.xml` | テスト設定のため |
| `config.php.example` | サンプルのみで実行不要 |
| `vendor/` | 現行アプリ本体では不要。将来、本番実行に必要な Composer パッケージを追加した場合のみ、ローカル生成後に FTP でアップロード |

### 配置時のパーミッション

ロリポップ配置後は、以下を基本値とする。詳細な設定手順は [13_デプロイ手順書.md](13_デプロイ手順書.md) を参照すること。

| 対象 | パーミッション |
|---|---:|
| `public_html/` / `api/` / `src/` / `assets/` などのディレクトリ | `705` |
| `index.php` / `api/analyze.php` / `src/*.php` | `604` |
| `assets/css/*.css` / `assets/js/*.js` | `604` |
| `.htaccess` を設置する場合 | `604` |
| `config.php` | `600` |
| `sql/*.sql` をアップロードする場合 | `600` |

`config.php` を `600` にして本番画面が動作しない場合のみ、`604` に変更して動作確認する。

---

## 5. Git 管理外ファイル

`.gitignore` で除外する代表例を以下に示す。

```text
php/config.php
php/vendor/
php/.phpunit.cache/
php/coverage-report/
```

---

## 6. アクセス制御対象

Web から直接参照させないパスは以下の通り。

- `php/config.php`
- `php/src/`
- `php/sql/`

これらは `.htaccess` により遮断する。

---

## 7. 関連ドキュメント

| ドキュメント | 内容 |
|---|---|
| [04_基本設計書.md](04_基本設計書.md) | システム構成・処理フロー |
| [05_詳細設計書.md](05_詳細設計書.md) | クラス設計・API 仕様 |
| [11_VSCode開発マニュアル.md](11_VSCode開発マニュアル.md) | ローカル開発手順 |
| [13_デプロイ手順書.md](13_デプロイ手順書.md) | 初回配置・更新反映手順 |
| [14_運用手順書.md](14_運用手順書.md) | キャッシュ運用・障害対応 |
